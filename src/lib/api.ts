export const API_BASE_URL = ""; //move to env ig

export async function fetchData(endpoint: string, options: RequestInit = {}) {
  try {
    //do api call here to the endp & ret
  } catch (error) {
    console.error(error);
    return null;
  }
}
